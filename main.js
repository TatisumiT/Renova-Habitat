const button = document.getElementById('Enviar');
const messageElement = document.getElementById('message');

button.addEventListener('click', function Enviar() {
        alert("Seu login foi registrado com sucesso.");
});

